# Nowgoal 赛事、水位与比分

## 多庄版本

### 过去7天历史补充

历史范围为北京时间2026-09-02至2026-09-08这7个完整自然日，仅纳入网站已标记完场且有比分的赛事；同时保留9月9日原有赛事。合并表为 `Nowgoal_2026-09-02_至_2026-09-09_多庄水位比分.xlsx`，按比赛ID去重。

最新后台程序改为 `nowgoal_history.py`，同时维护今天的多庄表与历史合并表；每30分钟先回填今天的比分，再用最多20分钟补采历史多庄数据。每轮约70%的补采时间获取各公司初盘与页面即时快照，其余时间逐公司核验赛前最后水位；未经历史核验的即时水位不会填入临场列。

```powershell
python nowgoal_history.py --start 2026-09-02 --end 2026-09-08 --current-date 2026-09-09 --watch --interval 1800
```

此为固定日期范围任务。历史计数、缺失情况与合并文件路径见 `history_summary.json`；日志在 `history_watcher.log`；PID在 `history_watcher.pid`。重启电脑后用上述命令恢复。不要同时运行旧版后台任务，避免重复抓取和写表。

打开 `Nowgoal_2026-09-09_多庄水位比分.xlsx`。主表每场比赛、每家有报价的公司各占一行，含初盘、按本公司历史核验的赛前最后水位、页面即时盘及比分。覆盖范围是源站返回的全部公司，不固定只抓某一家；尚未抓到的公司列表与历史均明确标注。

后台更新已切换使用 `nowgoal_multi.py`。多庄进度在 `multi_summary.json`，日志在 `multi_watcher.log`。更新命令：

```powershell
python nowgoal_multi.py --date 2026-09-09 --watch --interval 1800
```

每30分钟启动一轮比分回填，按本轮开始时间计算下一次运行，避免处理耗时累加。先更新比分并保存Excel，再补采多庄历史；每轮历史补采最多20分钟。单次请求间隔至少1.5秒；源站返回限制码后停止该轮网络请求，等下一轮重试。同场完场比分统一回填到各公司对应行。运行状态及下次时间见 `multi_automation_status.json`。不再设置72小时自动退出，全部待办完成后停止。此为本机后台任务，电脑需要保持开机、不休眠且联网；重启后需重新运行上述命令。

停止多庄后台进程时使用 `multi_watcher.pid`，并核对进程命令包含 `nowgoal_multi.py` 和 `--watch`。旧单庄表作为此前快照保留，不再由新程序更新。

本次范围：北京时间 2026-09-09 当天网站赛程的全部赛事，包含无赔率赛事。

打开 `Nowgoal_2026-09-09_赛事水位比分.xlsx`：

- **赛事水位比分**：每场一行，默认 Bet365 全场亚洲让球，香港水位。包括初盘、赛前最后记录、页面即时盘、比分和缺失说明。
- **赛前历史核验**：用于主表临场水位核验的赛前历史及时间。
- **各公司盘口快照**：附带源站返回的其他公司亚洲盘、大小球和胜平负数据；未逐公司核验临场时间。
- **说明**：数据口径和抓取统计。

盘口正数表示主队让球，负数表示主队受让。未开赛比赛的“赛前最后”列留空，当前水位放在“当前赛前”列。只有源站标记完场才填写终场比分。

临场水位取网站标记为赛前、未封盘、严格早于网站计划开赛时间的最后一条公开历史记录。若实际开球延迟，网站计划时间与实际开球时间可能有差异。无法核验的临场水位留空；不拿滚球水位代替。

手动重新抓取并回填：

```powershell
python nowgoal_collect.py --date 2026-09-09 --refresh
```

持续回填（每 5 分钟检查；开赛及完场时重抓水位；最长 72 小时）：

```powershell
python nowgoal_collect.py --date 2026-09-09 --watch
```

运行时需要电脑保持开机并联网。查看 `watcher.log` 和 `collection_summary.json` 了解进度。如果 Excel 正占用原文件，更新保存到 `Nowgoal_2026-09-09_最新.xlsx`。

如已启动后台回填，可用以下命令停止本项目的回填进程（校验 PID 与脚本后停止）：

```powershell
$collectorPid = [int](Get-Content -LiteralPath '.\watcher.pid')
$collectorProcess = Get-CimInstance Win32_Process -Filter "ProcessId = $collectorPid"
if ($collectorProcess.CommandLine -like '*nowgoal_collect.py*--watch*') {
    Stop-Process -Id $collectorPid
}
```

原始数据在 `evidence`，归并记录在 `collection_2026-09-09.json`，便于复查。没有报价、取消、推迟和接口失败均保留在表中，不伪造缺失值。程序依赖 Python 3 与 openpyxl（当前电脑已具备）。
